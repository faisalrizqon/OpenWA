import type { HeroImage, Perk, Testimonial, VideoContent } from "@prisma/client";

/** Prisma model types untuk koleksi konten storefront. */
export type HeroImageRow = HeroImage;
export type PerkRow = Perk;
export type TestimonialRow = Testimonial;
export type VideoRow = VideoContent;
