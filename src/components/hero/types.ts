/** Tinggi hero kompak — tidak satu layar penuh, katalog mulai terlihat. */
export const HERO_MIN_H = "min-h-[52dvh]";

export interface HeroProduct {
  name: string;
  image: string;
  timestamp: string;
}

export interface PerkItem {
  icon: string;
  title: string;
  description: string;
}
